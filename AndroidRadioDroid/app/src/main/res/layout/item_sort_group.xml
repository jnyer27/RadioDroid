<?xml version="1.0" encoding="utf-8"?>
<!-- Draggable row in the Sort Channels by Group screen. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingStart="8dp"
    android:paddingEnd="16dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- Drag handle — touch this to begin dragging the row -->
    <ImageView
        android:id="@+id/dragHandle"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_drag_handle"
        android:padding="8dp"
        android:contentDescription="Drag to reorder"
        android:background="?android:attr/selectableItemBackgroundBorderless" />

    <!-- Group letter badge -->
    <TextView
        android:id="@+id/sortGroupLetter"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:gravity="center"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="?colorPrimary"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="12dp"
        android:text="A" />

    <!-- Group label name (expands) -->
    <TextView
        android:id="@+id/sortGroupLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="14sp"
        android:text="Label" />

    <!-- Channel count -->
    <TextView
        android:id="@+id/sortGroupCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="?android:attr/textColorSecondary"
        android:layout_marginStart="8dp"
        android:text="0 ch" />

</LinearLayout>
